using System;
using Vala.Game.Framework;

void main () {
  for (var i = 0; i < 10000; ++i) {
    // Console.write_line (@"$i");
    // stdout.puts("hello\n");
    Console.write_line ("hello");
  }
  
}